---
import type { HTMLAttributes } from "astro/types";
import { tv, type VariantProps } from "tailwind-variants";

interface Props extends HTMLAttributes<"div">, VariantProps<typeof styles> {
  contentClass?: string;
  wrapperClass?: string;
}

export const styles = tv({
  base: "flex scroll-my-40 flex-row gap-32 rounded-lg bg-white px-16 py-40 sm:px-80",
  variants: {
    look: {
      gray: "bg-zfl-main20",
    },
  },
});

const contentStyles = tv({
  base: "flex flex-col gap-16",
});

const { contentClass, wrapperClass, look, ...attrs } = Astro.props;
---

<div class={styles({ look, class: wrapperClass })} {...attrs}>
  {
    Astro.slots.icon && (
      <div class="shrink-0">
        <slot name="icon" />
      </div>
    )
  }
  <div class={contentStyles({ class: contentClass })}><slot /></div>
</div>
