---
import type { HTMLAttributes } from "astro/types";
import { tv, type VariantProps } from "tailwind-variants";

interface Props extends HTMLAttributes<"mark">, VariantProps<typeof styles> {
  class?: string;
}

const styles = tv({
  base: "ds-label-02-reg inline-flex flex-row items-center gap-4 self-start rounded-md bg-transparent p-4 forced-colors:dark:bg-transparent forced-colors:dark:text-white forced-colors:dark:[forced-color-adjust:none]",
  variants: {
    look: {
      hint: "text-cosmic-blue-base",
      gray: "bg-lavender-base",
      white: "bg-white",
    },
  },
});

const { class: className, look, ...attrs } = Astro.props;
---

<mark class={styles({ look, className })} {...attrs}>
  <slot name="icon" />
  <slot />
</mark>
