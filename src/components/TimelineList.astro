<ol class="timeline list-unstyled">
  <slot />
</ol>

{
  /* use the is:global directive to ensure that direct child descendant selectors (>) can be used */
}
<style is:global>
  @reference "@/styles/global.css";

  .timeline {
    @apply relative scroll-my-40 space-y-32;

    h3 {
      margin-top: 1rem;
    }

    &::before {
      content: "";
      @apply bg-cosmic-blue-base absolute inset-0 left-9.5 w-1;
    }

    & > li {
      @apply relative pt-10 pb-20 pl-40;
    }
    & > li::before {
      position: absolute;
      content: "";
      @apply size-sm bg-cosmic-blue-base top-14 left-0 rounded-full outline-4 outline-white;
    }
  }
</style>
