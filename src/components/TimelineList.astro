<ol class="timeline list-unstyled">
  <slot />
</ol>

{
  /* use the is:global directive to ensure that direct child descendant selectors (>) can be used */
}
<style is:global>
  @reference "@/styles/global.css";

  .timeline {
    --timeline-padding: 24px;
    /* space to the left of the line so year badges can be centered on it */
    @apply relative scroll-my-40 space-y-32 pl-(--timeline-padding);

    h3 {
      margin-top: 1rem;
    }

    &::before {
      content: "";
      @apply bg-cosmic-blue-base absolute inset-0 w-1;
      left: calc(var(--timeline-padding) + 9.5px);
    }

    & > li {
      @apply relative pt-10 pb-20 pl-40;
    }
    & > li::before {
      position: absolute;
      content: "";
      @apply bg-cosmic-blue-base top-16 left-2 size-16 rounded-full outline-4 outline-white;
    }

    & > li.timeline-year-item {
      @apply relative min-h-32 pt-0 pb-8 pl-0;
    }
    & > li.timeline-year-item::before {
      content: none;
    }
    & > li.timeline-year-item > .timeline-year-badge {
      @apply absolute top-1/2 left-10 -translate-x-1/2 -translate-y-1/2;
    }
  }
</style>
