---
import { Icon } from "astro-icon/components";
import Card, { styles } from "@components/Card.astro";
import { ZFL_EMAIL, ZFL_PHONE } from "@config/constants";
import type { VariantProps } from "tailwind-variants";

type Props = VariantProps<typeof styles>;

const { look } = Astro.props;
---

<Card look={look}
  ><Icon
    name="ic:twotone-phone-enabled"
    class="text-zfl-main60 hidden size-80 -scale-x-100 sm:block"
    slot="icon"
  />
  <h2>Kontaktieren Sie uns</h2>
  <div class="space-y-8">
    <p>
      Wir unterstützen Sie gerne persönlich in allen Phasen der Regelungsarbeit.
    </p>
    <p>
      Telefon: <a class="text-link" href={ZFL_PHONE.url}>{ZFL_PHONE.display}</a>
    </p>
    <p>
      E-Mail: <a class="text-link" href={ZFL_EMAIL.url}>{ZFL_EMAIL.display}</a>
    </p>
  </div>
</Card>
