---
interface Props {
  class?: string;
}
const hasSubtitle = Astro.slots.has("subtitle");
---

<div class="breakout bg-zfl-main80 text-white pt-40 pb-48">
  <slot />
  {
    hasSubtitle && (
      <div class="mt-16 ds-subhead">
        <slot name="subtitle" />
      </div>
    )
  }
</div>
