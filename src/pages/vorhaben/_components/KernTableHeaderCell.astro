---
interface Props {
  scope?: "col" | "row";
  class?: string;
  colspan?: number | string;
  rowspan?: number | string;
}

const { scope = "col", class: className, ...rest } = Astro.props;
---

<th scope={scope} class:list={["kern-table__header", className]} {...rest}>
  <slot />
</th>
